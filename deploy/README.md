# Deployment Files

- `beszel/` contains local-only Beszel Hub/Agent Compose examples for monitoring and alert display.
- `guardian/` contains the local disposable Ubuntu service templates.
- `guardian-x86/` contains the Ubuntu x86_64 observe-only service templates.

Neither Guardian installer enables automatic actions. Do not expose the local Beszel examples publicly or use them as production deployment definitions.
