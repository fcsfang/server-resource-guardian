# Guardian transport defense: keep a hard floor for GFP_ATOMIC
# allocations on the TCP accept path under memory pressure (~3% RAM).
# Without it, accept-queue processing stalls when global free pages drop
# below the waterline (observed as SSH transport-stage timeouts under the
# pinned collapse form, 2026-09-23/24).
vm.min_free_kbytes = 120318
