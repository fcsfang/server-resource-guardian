# Copy this file to .env while the Beszel Add System dialog is still open.
# Start the agent first, then finish adding the system in the dialog.
# Keep the quotes around the public key because it contains spaces.
BESZEL_AGENT_TOKEN="replace-with-token-from-hub"
BESZEL_AGENT_KEY="ssh-ed25519 replace-with-public-key-from-hub"
