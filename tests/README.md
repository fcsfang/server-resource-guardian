# 测试目录

测试服务于产品开发，不负责产生路线任务。

## 当前规则

- 开发一个功能时，只补它直接需要的单元和集成测试。
- 每个里程碑完成时做一次整体验收。
- 修复缺陷后重跑同一个验收，不建立新的实验编号。
- 在本地可丢弃环境以外，不执行压力注入或真实动作。

统一回归命令：

```bash
bash scripts/check-repository.sh
```

测试通过不等于功能完成；功能还必须接入实际服务并能向用户演示。
