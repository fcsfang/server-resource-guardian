# 目标二(外部运维通道)实现进度评估

日期:2026-09-24
依据:ROADMAP.md 两段式验收契约(eb2e7b2)+ 2026-09-23/24 夜跑证据

## 按契约逐条打分

### 层 1 — 进入能力(能否进来)

| 契约要求 | 状态 | 证据 |
| --- | --- | --- |
| 分阶段探针(transport/auth/PAM/shell/首诊断) | ✅ 完成 | entry_probe2.py 三阶段归因,40 probes × 4 配置 |
| ≥20 probes × 2 runs/配置 | ✅ 完成 | idle(on)40/40;钉死(on)19/40;钉死(off)19/40 |
| on 不得降低进入率(vs off) | ✅ 通过 | on 19/40 == off 19/40,完全对称 |
| 每个失败必须归因到阶段 | ✅ 完成 | 全部归因 transport 层 |
| 防御后复测(OOMScoreAdjust 等) | 🔶 部分完成 | 进程级防御已部署验证(695804a),但钉死失败在 TCP accept 层,低于进程防御可达范围 |
| **通过线(≥95% 进入率)** | ❌ **未达到(钉死形态)** | 52.5% 失败率,远超 5% 上限 |

**层 1 判定**:契约的 Reporting rule 已预见了这种情形 — "若某主机级别无法达到通过线,如实报告该级别不可保"。WSL 级别的结论:钉死形态下进入能力不可保(on 与 off 对称,与 Guardian 无关);空闲与中等压力形态下 100% 可保。**传输层防御复测已做(2026-09-24):min_free_kbytes 2.7 倍提升 + cgroup TCP 保底统计上零改变(20/40 vs 19/40),失败形态不变 — 该失败是 WSL2 平台(vsock+动态内存)属性,非内核水线可调问题。最终定论:x86_64 原生复测。**

### 层 2 — 会话内任务时长(进来后多快干完)

| 契约要求 | 状态 | 证据 |
| --- | --- | --- |
| MTTA 总量(on ≤ off 的 50%) | ✅ **远超达成** | on ≈42s vs off 盲找 1106-1160s(≈26 倍,通过线只要求 2 倍)|
| MTTD ≤10s | 🔶 临界 | old-build 实测 ~41s(从 journalctl);原因:内存可用 5.3% 才到 CRITICAL 线;new-build gate 转换即时标记(753a454)预期 <10s 但**未在钉死窗口实测** |
| MTTI P95 ≤5s | ✅ 达成 | rescue top 0.34s(爆满相位实测) |
| 观察者效应有界(docker 操作 ≤1.5× off) | ❌ 未验证 | gate 已实现部署(41c8d1a/753a454)但钉死窗口实测因通道故障未完成(唯一未竟项) |
| 通知通道活性(降级 60s 内 ≥1 条消息) | ❌ 未验证 | 依赖层 2 补跑 |

### 产物与基础设施(契约的支撑面)

| 项 | 状态 |
| --- | --- |
| 钉死压力模型(可复用工具包) | ✅ collapse-pressure-model 入库(0da8a0c)|
| OOM 防御(sshd -1000/runtime -900/其余 -800) | ✅ 部署验证(695804a)|
| 审计分级瘦身 + gate 转换即时标记 | ✅ 实现+294 测试(41c8d1a/753a454)|
| 网关降级模式感知(抑制 stale 误报) | ✅ 实现(753a454)|
| WSL 部署升级到全部新能力 | ✅ live 验证 |
| PROGRESS.md 收益表更新 | ❌ 未做(等层 2 补跑数据) |

## 总评:目标二完成度 ≈ 70%

- **层 1:约 80%** — 度量体系、防御、对称归因全部落地;缺 x86 复测 + 钉死形态通过线未达(契约允许"如实报告不可保",但这不是"通过")。
- **层 2:约 60%** — MTTA/MTTI 核心指标已达标且余量大;缺 new-build 的 MTTD 实测、观察者效应有界验证、通知活性验证(三者在同一次补跑中完成)。

## 达成"宣布完成"的剩余清单(按优先级)

1. **层 2 补跑**(一次钉死窗口内完成三件事:new-build MTTD 实测 + docker 操作 1.5× 有界 + 通知活性)— 脚本全部就绪(task5_l2_new.sh),WSL 通道稳定时 ≈40 分钟。注意:WSL 通道在钉死形态下会反复死亡(两晚 4 次),层 2 探针须全部内闭环落盘。
2. **PROGRESS.md 更新**:收益数字 + 层 1"该级别不可保(防御复测已排除内核水线因素)"的如实陈述。
3. **x86 原生复测**(层 1 的最终定论需要它;ROADMAP 环境契约本来就把 x86 列为前置)— 现在它是唯一能改变层 1 结论的实验。
