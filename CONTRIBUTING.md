# 开发与交接规则

## 开始工作

1. 阅读 `README.md`、`ROADMAP.md`、`PROGRESS.md`。
2. 运行 `git status --short --branch`，保留已有改动。
3. 只处理当前里程碑中第一个未完成的用户功能。

## 开发原则

- 先把功能接入实际运行入口，再补直接相关的测试。
- 不为参数微调、单个异常输入或相似压力场景建立新任务或新实验。
- 每个里程碑只做一次整体验收；修复后重跑同一个验收。
- 不新增 Goal、P0、任务编号或第二份路线。
- 默认只观察；真实容器动作需要本次明确授权。
- 不提交凭据、生产原始日志、运行数据库和未脱敏数据。

## 完成一个功能

必须同时满足：

1. 功能接入实际服务，而不是只能运行单独脚本。
2. 用户能直接看到或操作结果。
3. 失败时不会产生真实危险动作。
4. 相关自动化测试通过。
5. `PROGRESS.md` 用普通语言更新。

## 汇报模板

```text
已完成：<用户能看到的功能>
可演示：<现在可以现场演示什么>
未完成：<还不能做什么>
下一步：<下一个明确交付物>
```

除非用户追问，不要用测试数量、内部类名、协议名或历史实验编号汇报项目进度。
