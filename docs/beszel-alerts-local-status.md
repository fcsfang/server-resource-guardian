# Beszel 三资源告警当前状态

更新时间：2026-09-21

状态：隔离 Hub 已完成初始化、Agent 登记和三资源告警触发/恢复演示。

## 已完成

- 本地 Beszel 已配置 CPU、内存和磁盘容量告警。
- 已确认当前 Beszel 版本没有双级严重度字段，也没有磁盘 I/O 阈值告警。
- 已实现本地告警配置计划、只读回读和脱敏记录工具。
- Beszel 告警不能调用 Guardian 动作服务。
- 已新增 `deploy/beszel/docker-compose.isolated.yml`：使用独立的 8091 端口、容器名和数据卷，默认不带通知目的地。
- 隔离实例页面和告警历史已看到 CPU、Memory、Disk 三条告警，三条均完成恢复；本次没有修改原有 8090 实例。
- 现场结果：CPU 85%/1 分钟，内存 85%/1 分钟，磁盘 90%/1 分钟；临时压力源和磁盘占用文件已清理。

本地验证值为 CPU 85%、内存 85%、磁盘 90%，只用于演示，不是生产阈值。

## 下一步

当前证据只覆盖本地页面和告警历史。隔离 Hub 未配置 SMTP/Webhook，也没有完成真实管理员收件测试；不能声称已有真实邮件接收端。隔离 Hub 已在本地 `guardian-t11-matrix` 的 8091 端口完成演示；三类告警均已完成 CPU、内存和磁盘触发、恢复和页面展示。

这项工作属于里程碑一的功能演示，不再单独创建实验编号。

## 边界

- Beszel 负责图表和告警，不负责选择或停止容器。
- Guardian 本地判断不能依赖 Beszel 是否在线。
- 真实邮件、IM 或短信通知需要渠道管理员批准、明确收件人，并在获批的非生产环境完成送达测试；这仍是里程碑一进入非生产前的外部授权项。
